// This function checks to see if the sign up email being submitted is blatantly fake or not
// The user will still need to verify the email is their using a confirmation link sent via email though

export default function inspectEmail(inp) {}
